/** @format */

import express from "express";

import { uploadMetadataToCollectionSchema } from "../schemas/Morfi.schema";

import { validate } from "../middleware/validate";
const router = express.Router();

export default router;
